{
  "name": "@olofsson/homey-inventory",
  "version": "0.4.0",
  "private": true,
  "description": "Structurally read-only Homey inventory exporter foundation",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "validate": "npm run build && npm test"
  },
  "dependencies": {
    "homey-api": "3.19.1"
  },
  "devDependencies": {
    "@types/node": "24.0.0",
    "typescript": "5.9.2"
  }
}
